// Re-export from centralized labels module for backward compatibility.
export {
  EXPENSE_SECTION_NAV,
  sectionFromSlug,
  sectionLabel,
  slugForSection,
  type ExpenseSectionId,
  type SectionNavItem,
} from "@/src/lib/labels";
